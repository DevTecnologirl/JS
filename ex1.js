//String
let variable = "Camy";

//Number
let numer = 2;

//Boolean
let verific = true;

//Array: Esta é uma estrutura que permite armazenar vários valores em um único referência.
let matriz = [1,'camy','steve',10];
console.log(matriz);

//Object: Isso pode ser qualquer coisa. Tudo em JavaScript é um objeto e pode ser armazenados em uma variável. Tenha isso em mente enquanto aprende.
let objeto = document.querySelector('h1');

/**Então, por que precisamos de variáveis? As variáveis são necessárias para fazer qualquer coisa interessante na programação. Se os valores não pudessem ser alterados, você não poderia fazer nada dinâmico, como personalizar uma mensagem de saudação ou alterar uma imagem exibida em uma galeria de imagens. */